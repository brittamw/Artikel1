CREATE TABLE Name
(
NameID int NOT NULL AUTO_INCREMENT,
NameVorname varchar(255), 
NameNachname varchar(255),
NameTrainID int,
PRIMARY KEY (NameID),
FOREIGN KEY (NameTrainID) REFERENCES Training(TrainID)
);

INSERT INTO Name (NameVorname, NameNachname, NameTrainID) values ('Britta', 'Walter', 101);

usw.