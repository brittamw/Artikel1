CREATE TABLE Training (TrainID int NOT NULL,
                       TrainWeek int,
                       TrainTag varchar(255),
                       TrainWorkID varchar(255)
		       PRIMARY KEY (TrainID),
		       FOREIGN KEY (TrainWorkID) REFERENCES Workout(WorkID)
			);

INSERT INTO Training(TrainID, TrainWeek, TrainTag, TrainWorkID) VALUES (101, 1,'Montag', 'legs1');

usw.

